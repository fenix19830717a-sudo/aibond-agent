{
  "mcpServers": {
    "aibond": {
      "command": "aibond-mcp",
      "args": [
        "--server", "https://aib2b.bond",
        "--token", "${AIBOND_API_KEY}"
      ],
      "env": {
        "AIBOND_API_KEY": "${AIBOND_API_KEY}"
      }
    }
  }
}
